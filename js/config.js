/* ==========================================================================
   config.js — Site Configuration
   Ramayana: Beyond What We Were Told
   ========================================================================== */

const SITE_CONFIG = {
    siteName: "Ramayana: Beyond What We Were Told",
    siteSubtitle: "Return to the Text. Examine the Evidence. Rediscover the Story.",
    visitorCounterEndpoint: "",  // Set to a real API endpoint for global counter
    searchEnabled: true,
    animationsEnabled: true,
    readingSpeed: 200,           // words per minute for reading time estimates
    shareText: "Explore the textual history of the Ramayana",
    shareHashtags: "Ramayana,TextualHistory"
};
